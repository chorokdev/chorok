import dico  # noqa


class Paginator:
    def __init__(self,
                 embeds: list[dico.Embed],
                 message: dico.Message,
                 timeout: int = 30) -> None:
        pass
